# Beginner Level – Exam 1

## Topic: JavaScript Basics (Very Important Foundations)

## Syllabus

This test covers:

1. Variables (`var`, `let`, `const`)  
2. Data types  
3. Operators  
4. `console.log`  
5. Simple conditions  

---

## Part A – Theory

### Q1. What is JavaScript? Where does it run?

**Answer:**  
JavaScript is a programming language used to create interactive and dynamic web applications.  
It runs mainly in web browsers and can also run on servers using environments like **Node.js**.

---

### Q2. Difference between `var`, `let`, and `const`

- **`var`**  
  - Function-scoped  
  - Can be re-declared  
  - Avoid using in modern JS

- **`let`**  
  - Block-scoped  
  - Value can change

- **`const`**  
  - Block-scoped  
  - Value cannot be reassigned

---

### Q3. What are primitive data types in JavaScript?

**Answer:**  
Primitive data types in JavaScript are:

1. `string`  
2. `number`  
3. `boolean`  
4. `undefined`  
5. `null`  
6. `symbol`  
7. `bigint`  

---

### Q4. What does `console.log()` do and why is it important?

**Answer:**  
- Prints output to the console of the browser.  
- Important because it helps us:  
  - Test if the program is working correctly  
  - Debug the code  
  - Understand program flow

---

### Q5. Difference between `==` and `===`

- **`==`**  
  - Allows type coercion  
  - Compares values only

- **`===`**  
  - No type coercion  
  - Compares both value **and** type
